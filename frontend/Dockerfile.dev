# Dev image — Vite dev server with hot-module replacement
FROM node:22-slim

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm install --ignore-scripts

EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
